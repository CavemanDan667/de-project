# flake8: noqa
# from pg8000.native import Connection


# def test_make_dim_design():
    # conn = Connection(user="mock_tote_db",
    #                   host="localhost",
    #                   password="postgres")
    # conn.run("DROP TABLE IF EXISTS dim_design;")
    # conn.run("CREATE TABLE dim_design (design_ID serial primary key, design_name VARCHAR(50), column_3 INT, last_updated TIMESTAMP);")
    # conn.run("INSERT INTO dim_design (column_1, column_2, column_3, last_updated) VALUES (1, 'one', 10, '2020-01-01 00:00:00'), (2, 'two', 20, '2020-01-01 00:00:00'), (3, 'three', 30, '2023-11-01 00:00:00');")
    

# test_db_seeder()
